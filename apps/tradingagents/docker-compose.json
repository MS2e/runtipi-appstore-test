{
  "schemaVersion": 2,
  "services": [
    {
      "name": "tradingagents",
      "image": "python:3.12-slim",
      "isMain": true,
      "internalPort": 8080,
      "command": "/app/start.sh",
      "environment": [
        {"name": "TRADINGAGENTS_LLM_PROVIDER", "value": "${TRADINGAGENTS_LLM_PROVIDER}"},
        {"name": "TRADINGAGENTS_API_KEY", "value": "${TRADINGAGENTS_API_KEY}"},
        {"name": "TRADINGAGENTS_DEFAULT_MODEL", "value": "${TRADINGAGENTS_DEFAULT_MODEL}"},
        {"name": "TRADINGAGENTS_OUTPUT_LANGUAGE", "value": "${TRADINGAGENTS_OUTPUT_LANGUAGE}"},
        {"name": "TRADINGAGENTS_INITIAL_TICKER", "value": "${TRADINGAGENTS_INITIAL_TICKER}"}
      ],
      "volumes": [
        {
          "hostPath": "${APP_DATA_DIR}/data/web",
          "containerPath": "/app/web",
          "readOnly": true
        },
        {
          "hostPath": "${APP_DATA_DIR}/data/server.py",
          "containerPath": "/app/server.py",
          "readOnly": true
        },
        {
          "hostPath": "${APP_DATA_DIR}/data/start.sh",
          "containerPath": "/app/start.sh",
          "readOnly": true
        }
      ],
      "restart": "unless-stopped"
    }
  ]
}
